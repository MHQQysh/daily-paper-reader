---
title: "ASAP: Attention-Shift-Aware Pruning for Efficient LVLM Inference"
title_zh: ASAP：面向高效 LVLM 推理的注意力偏移感知剪枝
authors: "Surendra Pathak, Bo Han"
date: 2026-03-15
pdf: "https://arxiv.org/pdf/2603.14549v1"
tags: ["query:llm"]
score: 9.0
evidence: 用于高效多模态大模型推理的免训练剪枝方案
tldr: "ASAP 针对大语言视觉模型（LVLM）推理中高分辨率视觉 Token 带来的计算瓶颈，提出了一种无需训练且兼容 KV-Cache 的剪枝方案。该方法通过动态双向软注意力掩码解决“注意力偏移”问题，并利用加权软合并减少语义冗余。在保持 LLaVA-NeXT-7B 约 99% 性能的同时，成功降低了约 80% 的计算量，实现了高效且无损的视觉上下文压缩。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.14549v1/fig-001.webp\", \"caption\": \"Table 1: Performance comparison of ASAP with other pruning strategies on LLaVA-1.5-13B. Ratio represents the FLOPs ratio (percentage of vanilla FLOPs retained). Avg refers to the average percentage performance across all benchmarks compared to the vanilla model. Best scores are highlighted in red.\", \"page\": 11, \"index\": 1, \"width\": 671, \"height\": 267}, {\"url\": \"assets/figures/arxiv/2603.14549v1/fig-002.webp\", \"caption\": \"Table 4: System efficiency comparison on LLaVA-1.5-7B. We evaluate the computational cost and memory footprint during the prefill phase.\", \"page\": 14, \"index\": 2, \"width\": 714, \"height\": 236}, {\"url\": \"assets/figures/arxiv/2603.14549v1/fig-003.webp\", \"caption\": \"Fig. 3: The two-stage ASAP architecture. After decoder layer L, a salience-guided bidirectional matrix retains top-k salient visual tokens. A feature-based similarity module then consolidates redundant tokens via salience-weighted merging and a salvage mechanism. This dual-filtering pipeline passes a compact, feature-dense visual set to layer L+ 1, strictly preserving all text and system prompts.\", \"page\": 8, \"index\": 3, \"width\": 725, \"height\": 362}, {\"url\": \"assets/figures/arxiv/2603.14549v1/fig-004.webp\", \"caption\": \"Fig. 5: Impact of integrating SG-BiMask in existing pruning frameworks.\", \"page\": 19, \"index\": 4, \"width\": 508, \"height\": 327}, {\"url\": \"assets/figures/arxiv/2603.14549v1/fig-005.webp\", \"caption\": \"Table 3: Performance comparison of ASAP with other pruning strategies on InternVL 2.5 8B. Avg refers to the average percentage performance across all benchmarks compared to the vanilla model. Best scores within each token budget are highlighted in red.\", \"page\": 12, \"index\": 5, \"width\": 692, \"height\": 219}, {\"url\": \"assets/figures/arxiv/2603.14549v1/fig-006.webp\", \"caption\": \"Table 2: Performance comparison of ASAP with other pruning strategies on LLaVA-NeXT-7B. Avg refers to the average percentage performance across all benchmarks compared to the vanilla model. Best scores are highlighted in red.\", \"page\": 12, \"index\": 6, \"width\": 650, \"height\": 259}, {\"url\": \"assets/figures/arxiv/2603.14549v1/fig-007.webp\", \"caption\": \"Fig. 2: Qualitative comparison of token selection. Retained tokens (top 128) are visible, while dropped tokens are darkened. FastV demonstrates a bottom spatial bias, missing the distant third car. ASAP successfully mitigates this bias, preserving critical background features to accurately count all cars.\", \"page\": 5, \"index\": 7, \"width\": 723, \"height\": 262}]"
motivation: 现有的 Token 缩减方法忽略了 LVLM 中的“注意力偏移”现象及 Token 间的语义冗余，导致推理效率与性能难以兼得。
method: 提出 ASAP 方案，结合动态双向软注意力掩码以精准筛选信息，并利用加权软合并技术融合高语义冗余的视觉 Token。
result: "在 LLaVA-NeXT-7B 上实现了约 80% 的计算量削减，同时保留了 99.02% 的原始性能。"
conclusion: ASAP 证明了通过感知注意力偏移和处理语义冗余，可以在不进行额外训练的情况下显著提升 LVLM 的推理效率。
---

## 摘要
虽然大型视觉语言模型（LVLMs）展示了卓越的多模态能力，但处理高分辨率视觉 Token 的二次方计算成本仍然是一个关键瓶颈。尽管最近的 Token 减少策略试图加速推理，但这些方法未能充分利用注意力值，且无法有效解决 Token 冗余问题。更关键的是，它们忽视了 LVLMs 中固有的“注意力偏移”现象，该现象会导致 Token 注意力分数产生偏差。在这项工作中，我们提出了 ASAP，一种新型的无需训练且兼容 KV 缓存（KV-Cache）的剪枝方案，全面解决了这些局限性。首先，我们通过利用动态双向软注意力掩码来缓解注意力偏移，确保选择真正具有信息量的 Token，而非基于朴素注意力的选择。其次，我们认为 Token 集合中高度的语义冗余会降低性能。因此，我们引入了一个加权软合并组件，用于合并语义相似的 Token，仅为后续层保留特征最密集的视觉补丁（patches）。ASAP 实现了几乎无损的视觉上下文压缩，在保留 LLaVA-NeXT-7B 原始性能 99.02% 的同时，大幅削减了约 80% 的计算 FLOPs。

## Abstract
While Large Vision-Language Models (LVLMs) demonstrate exceptional multi-modal capabilities, the quadratic computational cost of processing high-resolution visual tokens remains a critical bottleneck. Though recent token reduction strategies attempt to accelerate inference, such methods inadequately exploit attention values and fail to address token redundancy. More critically, they overlook the ``attention shift'' phenomenon inherent in LVLMs, which skews token attention scores. In this work, we propose ASAP, a novel training-free, KV-Cache-compatible pruning recipe that comprehensively addresses these limitations. First, we mitigate the attention shift by utilizing a dynamic bidirectional soft attention mask, ensuring the selection of genuinely informative tokens rather than naive attention-based selection. Second, we posit that high semantic redundancy within the token set degrades performance. We therefore introduce a weighted soft merging component that merges semantically similar tokens, preserving only the most feature-dense visual patches for subsequent layers. ASAP achieves virtually lossless compression of visual context, retaining 99.02% of the original LLaVA-NeXT-7B performance while aggressively slashing computational FLOPs by ~80%.

---

## 论文详细总结（自动生成）

### 论文总结：ASAP：面向高效 LVLM 推理的注意力偏移感知剪枝

#### 1. 核心问题与整体含义（研究动机和背景）
大型视觉语言模型（LVLMs）在处理高分辨率图像时，会产生大量的视觉 Token（例如 LLaVA-NeXT 可达 2304 个以上），这导致 Transformer 架构的计算成本随序列长度呈二次方增长，严重制约了推理效率。
现有的 Token 剪枝方法存在两个主要缺陷：
*   **注意力偏移（Attention Shift）：** 由于旋转位置嵌入（RoPE）的距离衰减特性，模型倾向于给位置靠后的视觉 Token 分配更高的注意力分数，而忽视了靠前但语义重要的 Token。
*   **语义冗余：** 图像中存在大量语义相似的区域（如天空、背景），简单的 Top-k 剪枝会保留大量重复信息或丢失关键细节。
**ASAP** 的研究动机在于提出一种无需训练、即插即用的方案，通过修正注意力偏差和消除冗余来提升推理效率。

#### 2. 方法论：核心思想与关键技术
ASAP 包含三个核心组件，旨在实现“注意力偏移感知”的视觉 Token 压缩：
*   **显著性引导的双向注意力（SG-BiMask）：** 
    *   **核心思想：** 放宽视觉 Token 之间的因果掩码限制，允许早期 Token “窥视”后期 Token。
    *   **技术细节：** 利用预 Softmax 对齐矩阵计算每个 Token 的原始显著性，通过 Min-Max 归一化得到显著性分布。引入动态加性惩罚矩阵 $M_{bidir}$，根据显著性分数调整未来 Token 的可见度，从而抵消 RoPE 带来的位置偏见。
*   **显著性加权合并（Salience-Weighted Consolidation）：**
    *   **核心思想：** 识别并融合语义冗余的 Token。
    *   **技术细节：** 计算 Token 间的余弦相似度，将相似度高于阈值的 Token 进行合并。合并过程采用基于显著性分数的凸组合加权聚合，确保特征向结构化显著的锚点集中，防止特征稀释。
*   **预算重新分配（Budget Reallocation）：**
    *   **核心思想：** 优化固定 Token 预算的利用率。
    *   **技术细节：** 合并冗余 Token 后会腾出预算位置，ASAP 从之前被剪枝的池中回收显著性最高的 Token 填补这些空位，以最大化语义多样性。

#### 3. 实验设计
*   **基准模型：** LLaVA-1.5-13B、LLaVA-NeXT-7B（LLaVA-1.6）以及 InternVL 2.5 8B。
*   **数据集/场景：** 涵盖了 VQAv2、GQA（组合推理）、ScienceQA（科学推理）、TextVQA（OCR）、MME（综合感知）、MMBench（逻辑推理）和 MMVet。
*   **对比方法（Baselines）：** 包括 FastV、LLaVA-PruMerge+、SparseVLM、DivPrune 和 VisPruner。
*   **评估指标：** 任务准确率、FLOPs 比例、KV Cache 存储占用、GPU 显存及推理延迟。

#### 4. 资源与算力
*   **算力说明：** 论文**未明确说明**具体的实验硬件（如 GPU 型号、数量）或具体的训练/推理总时长。
*   **方法特性：** 论文强调 ASAP 是**免训练（Training-free）**的，这意味着它不需要额外的 GPU 算力进行模型微调，可以直接加载预训练权重运行。在效率分析中提到，ASAP 引入的动态选择机制仅增加了约 **2ms** 的微小推理开销。

#### 5. 实验数量与充分性
*   **实验规模：** 论文在 3 种不同架构的模型上进行了测试，覆盖了超过 8 个主流多模态 Benchmark。
*   **消融实验：** 专门设计了消融实验来验证“双向注意力模块”和“加权合并/回收模块”的各自贡献，证明了组合方案的有效性。
*   **客观性与公平性：** 实验对比了不同 Token 保留预算（如 128、192、640、960 等）下的性能，并使用了理论 FLOPs 作为统一的效率衡量标准，实验设计较为全面、客观。

#### 6. 主要结论与发现
*   **高效压缩：** ASAP 在 LLaVA-NeXT-7B 上仅保留约 22% 的 Token，减少了约 **80% 的计算 FLOPs**，同时保留了 **99.02%** 的原始性能。
*   **超越基准：** 在某些任务（如 MMBench）上，ASAP 的性能甚至超过了未剪枝的原始模型（Supra-vanilla performance），这表明剪枝过程可能起到了去除视觉噪声的作用。
*   **解决偏移：** 实验定性分析证明，ASAP 成功纠正了 FastV 等方法存在的“底部空间偏见”，能够正确识别图像背景中的远端物体。

#### 7. 优点
*   **理论深刻：** 首次系统性地将 LVLM 中的注意力偏移归因于 RoPE 的距离衰减特性，并给出了数学解释。
*   **架构兼容：** 方案完全兼容 KV Cache，支持多轮对话推理，且无需对模型进行结构性修改或重新训练。
*   **性能卓越：** 在极高的压缩比下依然能保持近乎无损的精度，优于现有的所有免训练剪枝基准。
*   **即插即用：** 作为一个轻量级模块，可以轻松集成到现有的推理流水线中。

#### 8. 不足与局限
*   **推理开销：** 虽然 FLOPs 大幅下降，但动态计算显著性和相似度合并会带来额外的 CPU/GPU 调度开销（约 2ms 延迟），在极高性能要求的实时场景中需权衡。
*   **超参数依赖：** 方案涉及显著性阈值、合并阈值等超参数（如 $\lambda_{max}$），可能需要针对不同模型进行微调以达到最优效果。
*   **实验覆盖：** 实验主要集中在 LLaVA 系列和 InternVL，对于其他非 Transformer 架构或非 RoPE 编码的模型适用性尚未讨论。
*   **文本忽略：** 剪枝仅针对视觉 Token，未探讨视觉与文本 Token 协同剪枝的可能性。

（完）
