---
title: "FlatAttention: Dataflow and Fabric Collectives Co-Optimization for Large Attention-Based Model Inference on Tile-Based Accelerators"
title_zh: FlatAttention：基于瓦片架构加速器的大型注意力模型推理的数据流与织网集合通信协同优化
authors: "Chi Zhang, Luca Colagrande, Renzo Andri, Luca Benini"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.02110v1"
tags: ["query:flattn"]
score: 9.0
evidence: 分块加速器上的注意力数据流优化
tldr: 针对大模型（如MoE）推理中Attention计算占比增加及HBM带宽瓶颈问题，本文提出FlatAttention。这是一种专为瓦片式（Tile-based）加速器设计的并行数据流，通过协同优化片上网络集合通信原语与计算，显著减少HBM访问。在类GH200配置下，其性能优于FlashAttention-3，并在DeepSeek-v3端到端评估中实现了吞吐量和延迟的显著提升。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.02110v1/fig-001.webp\", \"caption\": \"Fig. 8: Runtime breakdown (bars) and average HBM BW utilization (star markers) for different MHA implementations and layer sizes. +Runtime not overlapped with matrix engine. ++Runtime not overlapped with either vector or matrix engine. *Implementations without double buffering.\", \"page\": 10, \"index\": 1, \"width\": 519, \"height\": 346}, {\"url\": \"assets/figures/arxiv/2604.02110v1/fig-002.webp\", \"caption\": \"Fig. 9: Runtime breakdown for different (square) flattening scales and layer sizes. Percentage labels above the bars indicate the average utilization of the matrix engine when active. +Runtime not overlapped with matrix engine. ++Runtime not overlapped with either vector or matrix engine.\", \"page\": 10, \"index\": 2, \"width\": 519, \"height\": 348}, {\"url\": \"assets/figures/arxiv/2604.02110v1/fig-003.webp\", \"caption\": \"Fig. 10: A general tiling and group-scaling strategy for FlatAttention.\", \"page\": 10, \"index\": 3, \"width\": 508, \"height\": 292}, {\"url\": \"assets/figures/arxiv/2604.02110v1/fig-004.webp\", \"caption\": \"Fig. 3: (a) SoA LLM model architecture overview and schematic for (b) MHA in prefill (c) MHA in auto-regressive decoding as well as (d) GQA in auto-regressive decoding.\", \"page\": 4, \"index\": 4, \"width\": 1055, \"height\": 271}, {\"url\": \"assets/figures/arxiv/2604.02110v1/fig-005.webp\", \"caption\": \"TABLE III: Related Work Comparison\", \"page\": 12, \"index\": 5, \"width\": 520, \"height\": 252}, {\"url\": \"assets/figures/arxiv/2604.02110v1/fig-006.webp\", \"caption\": \"Fig. 13: (a) DeepSeek-v3-671B decoding performance on 64-accelerator system comparing using FlatAttention and FlashMLA dataflow, as well as (b) runtime breakdown for decoding with 256 batch size per chip. Effect of expert parallelism: (c) Performance on the wafer-scale system; (d) D2D communication overhead at batch size of 256.\", \"page\": 12, \"index\": 6, \"width\": 1006, \"height\": 257}, {\"url\": \"assets/figures/arxiv/2604.02110v1/fig-007.webp\", \"caption\": \"TABLE II: DeepSeek-v3-671B Decoding Performnace Comaprision with SoA GPU/NPU Solutions\", \"page\": 12, \"index\": 7, \"width\": 1081, \"height\": 204}]"
motivation: 随着MoE等大模型的发展，Attention计算成为性能瓶颈，且瓦片式架构加速器急需高效的数据流管理以克服HBM访问限制。
method: 提出FlatAttention数据流，利用集成在片上网络织物中的集合通信原语，实现计算与通信的深度融合以最小化内存访问。
result: 在32x32瓦片配置下，FlatAttention比FlashAttention-3快4.1倍，HBM流量降低16倍，并在DeepSeek-v3推理中提升了1.9倍吞吐量。
conclusion: FlatAttention通过软硬件协同优化，为大规模瓦片式加速器上的Attention计算提供了高效且通用的解决方案。
---

## 摘要
在混合专家（MoE）模型的推理过程中，注意力机制在总计算量中所占的比例日益增大，使得高效加速变得至关重要。新兴的用于大模型推理的领域特定加速器正转向芯片级和晶圆级基于瓦片（Tile-based）的架构。瓦片包含大型矩阵和向量引擎，并通过片上互连进行连接，支持瓦片间通信以缓解瓦片到主存的流量瓶颈。因此，数据流管理对于实现高利用率至关重要。我们提出了 FlatAttention，一种适用于基于瓦片加速器的现代注意力变体的数据流。FlatAttention 通过利用集成在片上网络织网中的集合原语，最大限度地减少了昂贵的高带宽内存（HBM）访问，实现了高达 92.3% 的利用率，相比 FlashAttention-3 提速 4.1 倍，并将 HBM 流量降低了 16 倍。在峰值性能与 NVIDIA GH200 相当的 32x32 瓦片配置下，FlatAttention 可推广至多种注意力变体，在计算受限型注意力中实现了 86% 的平均利用率，在内存受限型中实现了 78% 的 HBM 带宽利用率，相比 GH200 上的注意力实现平均提速 1.9 倍。最后，我们在晶圆级多芯片系统上评估了使用 FlatAttention 的端到端 DeepSeek-v3 FP8 解码，尽管系统峰值性能比现有最先进方案低 1.5 倍，但仍实现了 1.9 倍的系统吞吐量提升和 1.4 倍的用户人均 token 输出延迟降低。

## Abstract
Attention accounts for an increasingly dominant fraction of total computation during inference for mixture-of-experts (MoE) models, making efficient acceleration critical. Emerging domain-specific accelerators for large model inference are shifting toward chip-scale and wafer-scale tile-based architectures. Tiles contain large matrix and vector engines and are connected through on-chip interconnects, which support tile-to-tile traffic to reduce the tile-to-main-memory traffic bottleneck. Hence, dataflow management is crucial to achieve high utilization. We propose FlatAttention, a dataflow for modern attention variants on tile-based accelerators. FlatAttention minimizes expensive high-bandwidth memory (HBM) accesses by exploiting collective primitives integrated into the on-chip network fabric, achieving up to 92.3% utilization, 4.1x speedup over FlashAttention-3, and 16x lower HBM traffic. On a 32x32 tile configuration with peak performance comparable to NVIDIA GH200, FlatAttention generalizes across multiple attention variants, achieving an average of 86% utilization for compute-bound attentions and 78% HBM bandwidth utilization for memory-bound ones, resulting in an average 1.9x speedup over attention implementations on GH200. Finally, we evaluate end-to-end DeepSeek-v3 FP8 decoding with FlatAttention on a wafer-scale multi-die system, achieving a 1.9x improvement in system throughput and a 1.4x reduction in per-user token output latency, despite operating with 1.5x lower peak system performance compared to the state-of-the-art solution.

---

## 论文详细总结（自动生成）

这是一份关于论文《FlatAttention: Dataflow and Fabric Collectives Co-Optimization for Large Attention-Based Model Inference on Tile-Based Accelerators》的结构化深入分析总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）从传统的密集型架构转向混合专家模型（MoE，如 DeepSeek-v3），计算瓶颈发生了转移。在 MoE 模型中，前馈网络（FFN）的计算量被显著压缩，导致**注意力机制（Attention）在推理总计算量中的占比激增**（在 DeepSeek-v3 解码阶段高达 71%）。
*   **现有瓶颈**：即便是在 NVIDIA GH200 等顶级 GPU 上，现有的优化方案（如 FlashAttention-3、FlashMLA）也仅能达到峰值性能的 26%-64%，且受限于昂贵的 HBM（高带宽内存）访问。
*   **硬件趋势**：新兴的 AI 加速器正转向“瓦片式”（Tile-based）架构（如晶圆级芯片），这些架构由大量包含矩阵/向量引擎的瓦片组成，通过片上网络（NoC）互连。
*   **核心目标**：如何通过**数据流（Dataflow）与片上网络集合通信（Collectives）的协同优化**，在瓦片式加速器上最大化利用率并最小化 HBM 流量。

### 2. 论文提出的方法论：FlatAttention
FlatAttention 的核心思想是将多个瓦片（Tiles）视为一个统一的逻辑计算单元（Group），通过共享局部内存（L1）来处理更大的注意力块。

*   **核心技术细节**：
    *   **平坦化并行（Flattening）**：不同于 FlashAttention 将任务限制在单个瓦片内，FlatAttention 将一个注意力块分布在 $N \times N$ 个瓦片上，利用聚合的 L1 缓存容量，从而将 HBM I/O 复杂度降低 $N$ 倍。
    *   **硬件级集合通信原语**：利用 NoC 织网（Fabric）支持的硬件级多播（Multicast）和规约（Reduction）。例如，在计算 Softmax 时，通过行内硬件规约快速获取全局最大值和分母，避免了昂贵的软件同步。
    *   **异步调度（Asynchronous Schedule）**：通过双缓冲和多头并发执行，将矩阵引擎（Matrix Engine）的运算与向量引擎（处理 Softmax）及 DMA 数据传输完全重叠，确保计算核心几乎不空转。
    *   **通用性适配**：通过“权重吸收”（Weight-absorption）等数学变换，将复杂的 MLA（多头潜在注意力）和 GQA（分组查询注意力）统一映射到 FlatAttention 数据流中。

### 3. 实验设计
*   **场景与数据集**：涵盖了 LLM 推理的两个关键阶段：**Prefill（预填充）**和 **Decode（解码）**。
*   **Benchmark（基准）**：
    *   **硬件基准**：NVIDIA GH200 GPU（FP16 峰值 988 TFLOPS，HBM 4TB/s）。
    *   **软件基准**：FlashAttention-2、FlashAttention-3 以及 DeepSeek 官方的 FlashMLA。
*   **对比方法**：
    *   在相同的瓦片架构上对比了 FlashAttention 数据流。
    *   对比了不同通信实现：软件顺序通信（SW.Seq）、软件树状通信（SW.Tree）与硬件织网通信（HW）。
*   **端到端评估**：在 64 芯片组成的晶圆级系统上运行完整的 **DeepSeek-v3-671B** 解码任务。

### 4. 资源与算力
*   **模拟平台**：使用了基于 **GVSoC** 的事件驱动模拟框架，并针对 RedMulE 矩阵引擎和 FlooNoC 片上网络进行了 RTL 级的周期精确校准（误差在 0.17% - 12% 以内）。
*   **硬件配置**：
    *   **单芯片**：32x32 瓦片阵列，运行频率 965MHz（FP16）至 1.9GHz（FP8），配备 HBM4。
    *   **系统级**：64 个加速器组成的晶圆级多芯片系统，通过 1TB/s 带宽的 D2D（芯片间）链路互连。
*   **训练/推理时长**：论文侧重于推理性能评估，未提及模型训练时长。

### 5. 实验数量与充分性
论文进行了多维度的详尽实验：
*   **微观基准测试**：测试了不同序列长度（512 到 4096）、不同头维度（64, 128）下的算子表现。
*   **消融实验**：分析了瓦片分组规模（4x4 到 32x32）对利用率的影响，识别了“过度平坦化”（Over-flattening）导致的性能下降点。
*   **并行策略探索**：对比了流水线并行（PP）、专家并行（EP）及其混合模式在晶圆级系统上的表现。
*   **充分性评价**：实验覆盖了从底层算子到顶层端到端系统的全栈评估，对比对象包含了目前工业界最先进的 GPU 方案，具有很高的客观性和说服力。

### 6. 论文的主要结论与发现
*   **性能飞跃**：在相同硬件条件下，FlatAttention 比 FlashAttention-3 提速 **4.1 倍**，HBM 流量降低 **16 倍**。
*   **高利用率**：在计算受限场景下实现 **86%** 的平均利用率；在内存受限场景下实现 **78%** 的 HBM 带宽利用率。
*   **超越顶级 GPU**：在与 GH200 的对比中，FlatAttention 在多种注意力变体上平均提速 **1.9 倍**。
*   **端到端优势**：对于 DeepSeek-v3-671B，尽管系统峰值算力比 96 台 H800 组成的集群低 1.5 倍，但通过 FlatAttention 实现了 **1.9 倍的吞吐量提升**和 **1.4 倍的延迟降低**。

### 7. 优点（亮点）
*   **软硬协同优化**：不只是纯算法优化，而是深入探讨了 NoC 硬件原语如何支撑数据流，为未来 AI 芯片设计提供了参考。
*   **极高的通用性**：成功证明了该数据流可以无缝支持 MHA、GQA、MLA 等所有主流注意力变体。
*   **解决实际痛点**：直接针对 DeepSeek-v3 等最新开源大模型的架构特点进行优化，具有极强的实战价值。

### 8. 不足与局限
*   **硬件依赖性**：FlatAttention 高度依赖于 NoC 对硬件级集合通信的支持，在缺乏此类特性的传统多核处理器或旧版 GPU 上难以发挥作用。
*   **互连拓扑敏感**：实验发现 2D Mesh 拓扑在超大规模并行时（如 EP64）会产生较大的多跳延迟，论文虽提出了问题，但未深入探讨更优的晶圆级拓扑结构。
*   **静态调度限制**：目前的评估基于相对静态的任务映射，对于动态负载（如不同长度的请求混合）的鲁棒性仍有待进一步验证。

（完）
