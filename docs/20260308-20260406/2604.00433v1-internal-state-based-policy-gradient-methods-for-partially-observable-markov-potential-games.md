---
title: Internal State-Based Policy Gradient Methods for Partially Observable Markov Potential Games
title_zh: 基于内部状态的部分可观测马尔可夫势博弈策略梯度方法
authors: "Wonseok Yang, Thinh T. Doan"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.00433v1"
tags: ["query:marl"]
score: 9.0
evidence: 针对马尔可夫势博弈的基于内部状态的自然策略梯度方法
tldr: 本研究针对部分可观测马尔可夫势博弈（POMPGs）中的多智能体强化学习难题，提出了一种基于内部状态的自然策略梯度方法。通过引入公共信息框架和有限状态控制器来压缩历史信息，解决了去中心化决策和维度灾难问题。研究建立了非渐近收敛界，并证明其由统计误差和近似误差组成。仿真结果表明，该方法在多种部分可观测环境下均优于仅依赖当前观测的策略。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.00433v1/fig-001.webp\", \"caption\": \"\", \"page\": 4, \"index\": 1, \"width\": 3000, \"height\": 1879}, {\"url\": \"assets/figures/arxiv/2604.00433v1/fig-002.webp\", \"caption\": \"\", \"page\": 4, \"index\": 2, \"width\": 1256, \"height\": 480}]"
motivation: 旨在解决部分可观测马尔可夫势博弈中由于信息不完全、去中心化决策及维度爆炸带来的学习挑战。
method: 结合公共信息框架与有限状态控制器来压缩累积信息，并采用基于内部状态的自然策略梯度算法寻找纳什均衡。
result: 理论上推导出了包含统计误差和有限状态近似误差的非渐近收敛界，实验中验证了该方法在复杂环境下的优越性。
conclusion: 引入内部状态能有效缓解部分可观测性带来的负面影响，为解决大规模多智能体势博弈提供了理论支撑和实用方案。
---

## 摘要
本文研究了部分可观测马尔可夫势博弈中的多智能体强化学习。由于部分可观测性、去中心化信息以及维数灾难，解决这一问题具有挑战性。首先，为了应对前两个挑战，我们利用了公共信息框架，该框架允许智能体基于共享信息和局部信息进行决策。其次，为了确保计算的可处理性，我们研究了一种压缩累积信息的内部状态，以防止其随时间无限增长。随后，我们实现了一种基于内部状态的自然策略梯度方法，用于寻找马尔可夫势博弈的纳什均衡。我们的主要贡献是为该方法建立了一个非渐近收敛界。我们的理论界可分解为两个具有解释性的部分：一个是在标准马尔可夫势博弈中也会出现的统计误差项，以及一个捕捉使用有限状态控制器所产生的近似误差项。最后，在多个部分可观测环境下的仿真表明，与仅使用当前观测值的设定相比，所提出的使用有限状态控制器的方法在性能上取得了持续的提升。

## Abstract
This letter studies multi-agent reinforcement learning in partially observable Markov potential games. Solving this problem is challenging due to partial observability, decentralized information, and the curse of dimensionality. First, to address the first two challenges, we leverage the common information framework, which allows agents to act based on both shared and local information. Second, to ensure tractability, we study an internal state that compresses accumulated information, preventing it from growing unboundedly over time. We then implement an internal state-based natural policy gradient method to find Nash equilibria of the Markov potential game. Our main contribution is to establish a non-asymptotic convergence bound for this method. Our theoretical bound decomposes into two interpretable components: a statistical error term that also arises in standard Markov potential games, and an approximation error capturing the use of finite-state controllers. Finally, simulations across multiple partially observable environments demonstrate that the proposed method using finite-state controllers achieves consistent improvements in performance compared to the setting where only the current observation is used.

---

## 论文详细总结（自动生成）

这是一份关于论文《Internal State-Based Policy Gradient Methods for Partially Observable Markov Potential Games》的深度结构化总结：

### 1. 核心问题与研究动机
该论文聚焦于**部分可观测马尔可夫势博弈（POMPGs）**中的多智能体强化学习（MARL）问题。
*   **挑战**：在现实场景（如搜救、自动驾驶）中，智能体通常只能获得局部观测，且决策是去中心化的。这导致了三个主要难题：部分可观测性、去中心化信息流以及随时间呈指数增长的信息维度（维数灾难）。
*   **研究目标**：开发一种计算上可行的算法，能够在不依赖环境精确动力学模型的情况下，通过有限的内存表示来寻找多智能体系统的纳什均衡（NE）。

### 2. 方法论：核心思想与技术细节
论文提出了一种**基于内部状态的自然策略梯度（NPG）方法**，其核心组成部分包括：
*   **公共信息框架（Common Information Framework）**：将去中心化 POMDP 转化为等效的中心化信念 MDP。智能体共享一部分观测和动作到公共内存，结合局部观测进行决策。
*   **有限内部状态（Internal States）**：为了解决信念空间连续且不可数的问题，引入有限集合 $W$（共享内部状态）和 $L_i$（局部内部状态）。通过预定义的压缩函数（如 RNN 或滑动窗口）将无限增长的历史信息压缩为有限维度的内部状态。
*   **有限状态控制器（FSC）**：将策略限制在基于内部状态的类中，从而保证计算的可处理性。
*   **内部状态 NPG 算法**：
    1.  使用 Softmax 参数化策略。
    2.  利用 Fisher 信息矩阵的逆来调整梯度方向，考虑策略空间的几何结构。
    3.  所有智能体同步更新策略参数，更新步长由优势函数（Advantage Function）决定。
*   **理论贡献**：证明了该算法的非渐近收敛性，收敛界为 $O(1/\sqrt{T}) + \epsilon_{FSC}$。其中 $\epsilon_{FSC}$ 是由于使用有限状态近似真实信念而产生的误差。

### 3. 实验设计
研究者在 **POSGGym** 基准测试库中选择了三个典型的部分可观测环境进行验证：
*   **MATiger（多智能体老虎问题）**：经典的协作与竞争权衡场景，需要智能体通过“听”来积累信息。
*   **MABC（多路访问广播信道）**：节点需协调发送时机以避免冲突，观测具有噪声。
*   **LBF（基于等级的觅食）**：智能体在网格中导航并协作收集食物，视场受限。
*   **对比基准（Baseline）**：
    *   **反应式策略（Reactive Policy）**：仅依赖当前时刻的观测值。
    *   **有限内存策略（Finite-Memory Policy）**：即本文提出的基于内部状态的方法（实验中窗口长度 $t_w=2$）。
    *   算法实现采用了带有参数共享的独立信任区域策略优化（ITRPO）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的硬件配置（如 GPU 型号、数量）以及具体的训练时长。
*   **实现细节**：提到使用了神经网络（NN）来近似策略函数和价值函数，以提高算法的可实现性。

### 5. 实验数量与充分性
*   **实验规模**：在 3 个不同的环境上进行了实验，展示了学习曲线（Return vs. Iterations）和智能体行为轨迹图。
*   **充分性评价**：
    *   **客观性**：实验结果清晰地展示了内存（内部状态）对性能的提升，尤其是在 MABC 环境中，反应式策略完全失效，证明了方法的必要性。
    *   **局限性**：实验主要集中在协作/共同奖励任务上。虽然理论适用于势博弈（Potential Games），但实验覆盖的博弈类型相对单一，缺乏更大规模智能体数量（如数十个智能体）的扩展性测试。

### 6. 主要结论与发现
*   **性能提升**：基于内部状态的策略在所有测试环境中均显著优于仅依赖当前观测的策略。
*   **内存的必要性**：在高度依赖历史信息的任务（如 MABC）中，没有内部状态压缩的智能体无法学会有效协作。
*   **理论验证**：实验观察到的收敛趋势与理论推导的 $O(1/\sqrt{T})$ 收敛界相吻合。
*   **近似误差**：有限状态控制器虽然引入了近似误差 $\epsilon_{FSC}$，但在实际应用中通过增加内部状态的表达能力（如增加窗口长度或使用更复杂的压缩器）可以有效减小该误差。

### 7. 优点
*   **理论完备**：为部分可观测环境下的势博弈提供了首个非渐近收敛分析，填补了该领域的理论空白。
*   **结构化设计**：结合了公共信息框架和有限状态控制器，既保留了去中心化执行的特性，又解决了信息爆炸问题。
*   **可解释性**：收敛界被分解为统计误差和近似误差，为后续优化内部状态表示提供了明确的方向。

### 8. 不足与局限
*   **内部状态函数的选择**：论文假设压缩函数（$\phi, \chi$）是预先给定的。在复杂任务中，如何自动学习最优的压缩表示（而非手动设置窗口大小）仍是一个挑战。
*   **势博弈的限制**：虽然势博弈涵盖了许多重要场景，但它仍是马尔可夫博弈的一个子类，该方法在一般项博弈（General-sum Games）中的适用性尚未证明。
*   **计算开销**：虽然解决了维度灾难，但维护共享内部状态和局部状态在通信带宽和存储上仍有一定开销，论文未深入探讨通信受限下的表现。

（完）
