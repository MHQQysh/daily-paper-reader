---
title: "98$\\times$ Faster LLM Routing Without a Dedicated GPU: Flash Attention, Prompt Compression, and Near-Streaming for the vLLM Semantic Router"
title_zh: 无需专用 GPU 的 98 倍速 LLM 路由：针对 vLLM 语义路由器的 Flash Attention、提示词压缩与近流式处理
authors: "Xunzhuo Liu, Bowei He, Xue Liu, Andy Luo, Haichen Zhang, Huamin Chen"
date: 2026-03-13
pdf: "https://arxiv.org/pdf/2603.12646v1"
tags: ["query:flattn"]
score: 9.0
evidence: 利用 Flash Attention 实现更快的 LLM 路由
tldr: 针对LLM路由系统在共享GPU环境下内存占用高、延迟大的挑战，本文提出了三阶段优化方案。通过在AMD ROCm上实现自定义Flash Attention、引入无需神经推理的经典NLP提示词压缩技术，以及优化数据流处理，实现了98倍的端到端加速。该方案将16K长度的路由延迟降至108ms，且显存占用低于800MB，使路由系统能与vLLM高效共存而无需专用GPU。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.12646v1/fig-001.webp\", \"caption\": \"Fig. 3. Attention memory per classifier session vs. sequence length. SDPA exceeds the ∼718 MB available (dashed line) at ∼6K tokens. FA stays below but grows linearly. With compression, all inputs are reduced to 512 tokens, yielding a constant footprint regardless of original prompt length.\", \"page\": 7, \"index\": 1, \"width\": 500, \"height\": 318}, {\"url\": \"assets/figures/arxiv/2603.12646v1/fig-002.webp\", \"caption\": \"TABLE XIII GPU RESOURCE REQUIREMENTS OF LLM ROUTING SYSTEMS. OUR ROUTER IS THE ONLY ONE DESIGNED TO CO-LOCATE ON THE SERVING GPU.\", \"page\": 10, \"index\": 2, \"width\": 509, \"height\": 566}, {\"url\": \"assets/figures/arxiv/2603.12646v1/fig-003.webp\", \"caption\": \"TABLE I COMPARISON OF PROMPT COMPRESSION APPROACHES. COMPRESSION LATENCY IS FOR A 16K-TOKEN INPUT. OUR METHOD IS THE ONLY ONE REQUIRING ZERO NEURAL INFERENCE, KEEPING COMPRESSION OVERHEAD TO ∼19 MS AT 16K TOKENS VS. SECONDS FOR NEURAL METHODS.†\", \"page\": 3, \"index\": 3, \"width\": 1060, \"height\": 188}, {\"url\": \"assets/figures/arxiv/2603.12646v1/fig-004.webp\", \"caption\": \"TABLE VIII STAGED ACCELERATION AT 8K TOKENS. EACH STAGE BUILDS ON THE PREVIOUS; PER-STAGE AND CUMULATIVE SPEEDUPS ARE MEASURED E2E THROUGH ENVOY (N=10 PER RUN, 2–3 RUNS; §V-A). THE 16K ROW SHOWS SCALING BEYOND THE CPU-OPERABLE REGIME.\", \"page\": 8, \"index\": 4, \"width\": 714, \"height\": 295}, {\"url\": \"assets/figures/arxiv/2603.12646v1/fig-005.webp\", \"caption\": \"Fig. 4. E2E latency comparison (log scale). GPU+FA with streaming and compression achieves 50 ms at 8K tokens. At 16K+, only GPU+FA operates (108 ms at 16K; Table VIII).\", \"page\": 8, \"index\": 5, \"width\": 754, \"height\": 448}, {\"url\": \"assets/figures/arxiv/2603.12646v1/fig-006.webp\", \"caption\": \"TABLE X CLASSIFIER LATENCY UNDER CONCURRENT LOAD ON MI300X: RAW FA (TOP) VS. EFFECTIVE WITH COMPRESSION + STREAMING (BOTTOM). WITH COMPRESSION ALL INPUTS REDUCE TO ∼512 TOKENS. BOTTOM-PANEL C=1 VALUES AT 512–16K ARE MEASURED; 32K C=1 IS ESTIMATED. C>1 VALUES ARE DERIVED FROM C=1 OVERHEAD PLUS THE 512-TOKEN FA CONCURRENCY PROFILE.\", \"page\": 9, \"index\": 6, \"width\": 472, \"height\": 302}]"
motivation: 现有的LLM路由系统在处理长文本时因注意力机制的内存开销巨大，难以在不占用昂贵专用GPU资源的情况下与推理引擎共存。
method: 采用三阶段优化：在ROCm上实现Flash Attention降低内存复杂度，利用经典NLP算法压缩提示词，并通过自适应分块和零拷贝技术优化数据流。
result: 实现了端到端延迟从4918ms降至50ms（98倍加速），并将显存占用控制在800MB以内，支持高达32K的上下文处理。
conclusion: 该研究证明了通过系统级和算法优化，可以在极低资源消耗下实现高性能LLM路由，消除了对专用加速器的需求。
---

## 摘要
拦截 LLM 请求以进行安全分类、领域路由和 PII（个人身份信息）检测的系统级路由器必须兼具高速和轻量化运行的特点：它们应在为每个请求增加极低延迟的同时，不要求配备专用 GPU——这种昂贵的资源更适合用于 LLM 推理本身。当路由器与 vLLM 服务实例部署在同一块 GPU 上时，标准注意力机制的 $O(n^2)$ 内存占用使得长上下文分类（8K–32K token）变得不可能：在 8K token 时，三个并发分类器仅注意力掩码就需要约 4.5 GB 内存，远超 vLLM 剩余的可用内存。我们为 vLLM 语义路由器提出了三个阶段的优化方案，并在 AMD Instinct MI300X 上进行了基准测试，同时解决了延迟和内存问题。第一阶段：针对 ROCm 上的 ONNX Runtime 开发了自定义 CK Flash Attention 算子，将注意力内存占用从 $O(n^2)$ 降低到 $O(n)$，并将端到端（E2E）延迟从 4,918 ms 降低到 127 ms（38.7 倍提速），在 SDPA 发生显存溢出（OOM）的 8K–32K token 场景下实现正常运行。第二阶段：采用经典 NLP 提示词压缩技术（TextRank、位置加权、TF-IDF 和新颖性评分），在无需神经推理的情况下将所有输入压缩至约 512 token，无论原始提示词长度如何，都能将延迟和 GPU 内存占用限制在常数水平（E2E 延迟从 127 ms 降至 62 ms，2.0 倍提速）。第三阶段：通过自适应分块和零拷贝 JSON 实现近流式主体处理，消除了序列化开销（E2E 延迟从 62 ms 降至 50 ms，1.2 倍提速）。累计效果：实现 98 倍性能提升（从 4,918 ms 降至 50 ms），16K token 的路由仅需 108 ms，且路由器总 GPU 占用低于 800 MB——小到足以与 LLM 服务共享 GPU，从而消除了对专用加速器的需求。第一阶段针对 AMD ROCm（NVIDIA GPU 已通过 cuDNN 支持 FlashAttention）；第二和第三阶段与硬件无关。

## Abstract
System-level routers that intercept LLM requests for safety classification, domain routing, and PII detection must be both fast and operationally lightweight: they should add minimal latency to every request, yet not require a dedicated GPU -- an expensive resource better used for LLM inference itself. When the router co-locates on the same GPU as vLLM serving instances, standard attention's $O(n^2)$ memory makes long-context classification (8K--32K tokens) impossible: at 8K tokens, three concurrent classifiers need ${\sim}$4.5\,GB for attention masks alone, far exceeding the memory left by vLLM. We present three staged optimizations for the vLLM Semantic Router, benchmarked on AMD Instinct MI300X, that solve both the latency and the memory problem. \emph{Stage~1}: a custom CK Flash Attention operator for ONNX Runtime on ROCm reduces attention memory from $O(n^2)$ to $O(n)$ and end-to-end (E2E) latency from 4{,}918\,ms to 127\,ms (\textbf{38.7$\times$}), enabling 8K--32K tokens where SDPA OOMs. \emph{Stage~2}: classical NLP prompt compression (TextRank, position weighting, TF-IDF, and novelty scoring) reduces all inputs to ${\sim}$512 tokens without neural inference, capping both latency and GPU memory at a constant regardless of original prompt length (E2E 127$\to$62\,ms, \textbf{2.0$\times$}). \emph{Stage~3}: near-streaming body processing with adaptive chunking and zero-copy JSON eliminates serialization overhead (E2E 62$\to$50\,ms, \textbf{1.2$\times$}). Cumulatively: \textbf{98$\times$} improvement (4{,}918\,ms to 50\,ms), 16K-token routing in 108\,ms, and a total router GPU footprint under 800\,MB -- small enough to share a GPU with LLM serving and removing the need for a dedicated accelerator. Stage~1 targets AMD ROCm (NVIDIA GPUs already have FlashAttention via cuDNN); Stages~2 and~3 are hardware-agnostic.

---

## 论文详细总结（自动生成）

### 论文总结：针对 vLLM 语义路由器的 98 倍加速优化方案

#### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）应用向长文本（8K–32K tokens）演进，系统级路由层（负责安全过滤、意图分类、隐私检测等）面临严峻挑战。
*   **内存瓶颈**：标准注意力机制（SDPA）具有 $O(n^2)$ 的空间复杂度。在 8K tokens 时，仅注意力掩码就需约 1.5GB 显存，三个并发分类器会迅速耗尽 GPU 剩余内存，导致显存溢出（OOM）。
*   **延迟瓶颈**：CPU 推理速度过慢（8K tokens 需约 5 秒），无法满足路由层毫秒级的实时性要求。
*   **资源浪费**：为轻量级分类任务配备专用 GPU 成本极高。
**研究目标**：在不增加专用 GPU 的前提下，通过优化使路由器能与 vLLM 推理实例高效共存（Co-location），并显著降低长文本处理延迟。

#### 2. 论文提出的方法论：三阶段优化
论文提出了三个相互叠加的优化阶段：
*   **Stage 1: 针对 ROCm 的 CK Flash Attention**
    *   在 AMD ROCm 平台的 ONNX Runtime (ORT) 中实现了自定义算子。
    *   通过图重写（Graph Rewrite）将 $O(n^2)$ 的注意力子图替换为基于 AMD Composable Kernel (CK) 的 Flash Attention 算子。
    *   将内存复杂度降至 $O(n)$，并针对 ModernBERT 的交替局部/全局注意力模式进行了参数映射。
*   **Stage 2: 无需神经推理的提示词压缩**
    *   采用经典 NLP 算法组合进行抽取式压缩，将输入统一压缩至 ~512 tokens。
    *   **核心公式/信号**：
        1.  **TextRank**：基于余弦相似度图的中心性评分。
        2.  **位置加权 ($w_{pos}$)**：利用“中间迷失”现象，采用正弦函数模拟 U 型权重曲线，强化首尾信息。
        3.  **TF-IDF**：识别具有领域辨识度的稀有词。
        4.  **新颖性评分**：通过计算句子与文档质心的差异来捕获异常值（如注入攻击或 PII）。
*   **Stage 3: 近流式主体处理与零拷贝 JSON**
    *   **自适应分块**：通过首个数据块识别模型需求，决定是直接透传（Passthrough）还是累积处理。
    *   **零拷贝操作**：使用 `gjson`/`sjson` 进行字段提取和修改，避免了全量 JSON 序列化/反序列化的 $O(n)$ 开销。

#### 3. 实验设计
*   **场景与数据集**：
    *   **端到端延迟测试**：使用 500 到 16K tokens 的合成负载，包含越狱前缀、PII 令牌和特定领域内容。
    *   **准确率验证**：基于 8 个维基百科领域构建了 384 个测试用例，涵盖 4 种长度和 12 种信号位置组合。
*   **Benchmark 与对比方法**：
    *   **基准**：ONNX CPU (Buffered 模式)。
    *   **对比对象**：Candle CPU 框架、标准 GPU SDPA 模式、以及不同优化阶段的消融对比。
    *   **路由模型**：mmBERT-32K (270M 参数)。

#### 4. 资源与算力
*   **硬件平台**：单台服务器，配备 **1 块 AMD Instinct MI300X GPU** (192GB HBM3)。
*   **部署方式**：路由器与两个 vLLM 推理实例共用同一块 GPU。
*   **软件环境**：ROCm 7.0, ONNX Runtime 1.22.1, Go 1.24.1, Envoy v1.33。
*   **训练时长**：文中未提及模型训练时长，重点在于推理侧的系统优化。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了多组端到端延迟实验（N=10 次重复取均值）。
    *   针对并发负载（C=1 到 C=20）进行了扩展性分析。
    *   完成了 384 个真实内容的分类准确率对比实验。
*   **充分性评价**：实验设计较为充分，涵盖了从单请求延迟到高并发吞吐量的多维度评估，并进行了详细的消融实验以证明各阶段优化的贡献。准确率测试考虑了信号在文本中不同位置的影响，具有较好的客观性。

#### 6. 主要结论与发现
*   **性能飞跃**：三阶段累计实现 **98 倍**加速（8K tokens 延迟从 4,918ms 降至 50ms）。
*   **长文本支持**：在 16K tokens 下实现 108ms 路由，而传统方法此时已 OOM 或延迟不可接受。
*   **显存极低**：路由器总 GPU 占用控制在 **800MB 以内**，完美支持与 vLLM 共存。
*   **准确率提升**：发现提示词压缩具有“去噪”作用，使领域分类准确率从 53.1% 提升至 61.2%，PII 检测率从 78.5% 提升至 92.4%（因减少了长文本对注意力的稀释）。

#### 7. 优点
*   **软硬协同优化**：不仅在算法层面压缩，还深入底层算子解决了 AMD 生态下 ORT 缺乏 Flash Attention 的痛点。
*   **无需专用硬件**：极大地降低了企业部署 LLM 路由层的成本（每年每节点可节省数千美元的 GPU 租赁费用）。
*   **高效压缩**：提出的经典 NLP 压缩方案无需模型调用，延迟极低（16K 仅需 19ms），远快于基于 LLM 的压缩方法。

#### 8. 不足与局限
*   **硬件依赖性**：Stage 1 的优化目前高度绑定 AMD ROCm 平台，虽然 Stage 2/3 具有通用性，但核心加速能力在 NVIDIA 平台需另行适配（尽管 NVIDIA 已有成熟 FA 支持）。
*   **分词近似**：目前的提示词压缩使用字符长度启发式估算 Token 预算，而非精确的 Tokenizer，可能导致预算控制存在微小偏差。
*   **越狱检测权衡**：实验显示压缩后越狱检测准确率有所下降（70.8% -> 56.6%），虽然生产环境中可通过全量文本检测规避，但反映了压缩算法对对抗性样本的敏感性。

（完）
