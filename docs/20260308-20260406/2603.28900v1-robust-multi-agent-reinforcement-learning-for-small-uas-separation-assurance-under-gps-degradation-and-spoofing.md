---
title: Robust Multi-Agent Reinforcement Learning for Small UAS Separation Assurance under GPS Degradation and Spoofing
title_zh: GPS 性能下降与欺骗环境下小型无人机系统间隔保证的鲁棒多智能体强化学习
authors: "Alex Zongo, Filippos Fotiadis, Ufuk Topcu, Peng Wei"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28900v1"
tags: ["query:marl"]
score: 9.0
evidence: MARL中智能体与对手之间的零和博弈
tldr: 本研究针对小型无人机系统（sUAS）在GPS信号退化或欺骗环境下的间隔保证问题，提出了一种鲁棒多智能体强化学习（MARL）方法。通过将状态观测干扰建模为智能体与对手之间的零和博弈，推导出对抗扰动的闭式表达式，避免了复杂的对抗训练。该方法在保持线性计算复杂度的同时，显著提升了高密度无人机群在观测受损时的安全性，实现了极低的碰撞率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.28900v1/fig-001.webp\", \"caption\": \"Fig. 4. Snapshot of high-density sUAS traffic under the robust policy at corruption rate R “ 0.35. Aircraft (triangles) traverse structured routes through waypoints, with separation buffers shown as circles. Yellow indicates aircraft approaching loss of separation; green indicates nominal separation status. Labels display flight ID, speed (knots), and altitude (ft). Despite adversarial GPS perturbations, all aircraft maintain safe separation.\", \"page\": 7, \"index\": 1, \"width\": 512, \"height\": 256}, {\"url\": \"assets/figures/arxiv/2603.28900v1/fig-002.webp\", \"caption\": \"Fig. 1. An en-route airspace setting for sUAS package delivery. WP = WayPoint. The UASs flying in this airspace need to maintain safe separation when they are impacted by degraded or spoofed GPS signal. The network includes two routes that first cross at WP 1 (in red), then merge later at WP 7 (light red). Each route spans about 10 km, illustrating a compact yet realistic urban scenario for sUAS operations.\", \"page\": 1, \"index\": 2, \"width\": 462, \"height\": 364}]"
motivation: 解决小型无人机在GPS信号受损或被欺骗时，因观测状态不可靠而导致的空中交通安全风险。
method: 将状态干扰建模为零和博弈，推导出具有二阶精度的对抗扰动闭式解，并将其集成到MARL策略梯度算法中。
result: "在高密度仿真中，即使在35%的观测干扰下，该方法仍能保持近乎零的碰撞率，表现显著优于传统策略。"
conclusion: 该研究通过数学推导简化了对抗训练过程，为受限环境下的多无人机协同安全控制提供了高效且鲁棒的解决方案。
---

## 摘要
本研究针对 GPS 性能下降与欺骗环境下的小型无人机系统（sUAS）鲁棒间隔保证问题，提出了一种基于多智能体强化学习（MARL）的方法。在协同监视中，每架无人机（或智能体）广播其基于 GPS 获取的位置；当此类位置广播受到干扰时，整个观测到的空中交通状态将变得不可靠。我们将这种状态观测干扰建模为智能体与对手之间的零和博弈：对手以概率 R 扰动观测状态，以最大限度地降低每个智能体的安全性能。我们推导出了这种对抗性扰动的闭式表达式，从而完全绕过了对抗训练，并实现了随状态维度线性增长的评估效率。我们证明了该表达式能以二阶精度逼近真实的最坏情况对抗扰动。此外，我们界定了干净观测与受干扰观测之间的安全性能差距，证明在 Kullback-Leibler 正则化下，该差距随干扰概率的增加至多呈线性退化。最后，我们将闭式对抗策略集成到 MARL 策略梯度算法中，以获得智能体的鲁棒应对策略。在高密度 sUAS 仿真中，我们在干扰水平高达 35% 的情况下观察到了接近零的碰撞率，其性能优于未经对抗扰动训练的基准策略。

## Abstract
We address robust separation assurance for small Unmanned Aircraft Systems (sUAS) under GPS degradation and spoofing via Multi-Agent Reinforcement Learning (MARL). In cooperative surveillance, each aircraft (or agent) broadcasts its GPS-derived position; when such position broadcasts are corrupted, the entire observed air traffic state becomes unreliable. We cast this state observation corruption as a zero-sum game between the agents and an adversary: with probability R, the adversary perturbs the observed state to maximally degrade each agent's safety performance. We derive a closed-form expression for this adversarial perturbation, bypassing adversarial training entirely and enabling linear-time evaluation in the state dimension. We show that this expression approximates the true worst-case adversarial perturbation with second-order accuracy. We further bound the safety performance gap between clean and corrupted observations, showing that it degrades at most linearly with the corruption probability under Kullback-Leibler regularization. Finally, we integrate the closed-form adversarial policy into a MARL policy gradient algorithm to obtain a robust counter-policy for the agents. In a high-density sUAS simulation, we observe near-zero collision rates under corruption levels up to 35%, outperforming a baseline policy trained without adversarial perturbations.

---

## 论文详细总结（自动生成）

### 论文结构化总结：GPS 性能下降与欺骗环境下小型无人机系统间隔保证的鲁棒多智能体强化学习

#### 1. 核心问题与整体含义（研究动机和背景）
随着小型无人机系统（sUAS）在城市空域（如物流配送）的规模化应用，自主间隔保证（Separation Assurance）成为核心安全需求。然而，sUAS 高度依赖 GPS 进行定位和协同位置广播（Remote ID）。在城市环境中，GPS 信号常因多径效应产生 10-60 米的误差，且易受 5%-40% 成功率的恶意欺骗攻击。当 GPS 受损时，无人机不仅自身定位错误，还会接收到邻近机群的错误位置广播，导致整个观测状态不可靠。本论文旨在研究如何在观测状态受到对抗性干扰的情况下，通过鲁棒多智能体强化学习（MARL）确保飞行安全。

#### 2. 论文提出的方法论
论文将问题建模为智能体与对手之间的**零和博弈**，核心思想是让智能体在训练阶段就考虑到最坏情况的观测扰动。
*   **R-污染模型（R-contamination）**：假设观测状态以概率 $1-R$ 为真实状态，以概率 $R$ 被替换为不确定集内的对抗性观测值。
*   **闭式对抗扰动（核心技术）**：
    *   **定理 1**：利用价值函数 $V$ 的一阶泰勒展开，推导出最坏情况扰动的闭式解：$\Xi^{*,FO} = S_{t+1} - \kappa \cdot \text{sign}(\nabla V(S_{t+1}))$. 
    *   **优势**：该方法避免了传统对抗训练中复杂的内层优化（如梯度上升），将计算复杂度降至与状态维度呈线性关系。
    *   **定理 2**：证明了该闭式解对真实最坏情况的近似具有**二阶精度**。
*   **正则化策略梯度（Robust PPO）**：
    *   **不变性正则化（Invariance Regularization）**：通过 KL 散度约束，使策略在干净观测和对抗观测下输出一致。
    *   **锚定正则化（Anchoring Regularization）**：将鲁棒策略约束在预训练的标称策略附近，防止性能过度漂移。
*   **理论保证**：证明了在 KL 正则化下，预期性能损失随干扰概率 $R$ 呈线性增长，提供了明确的鲁棒性-性能权衡界限。

#### 3. 实验设计
*   **场景设置**：使用 **BlueSky** 空中交通模拟器，构建了一个 10km 长的城市航路场景，包含交叉点和汇合点。
*   **智能体模型**：模拟 Amazon MK30 无人机，采用点质量运动学模型，考虑风力干扰。
*   **Benchmark（基准）**：
    *   **Nominal Policy**：在无干扰环境下训练的标准 PPO 策略。
*   **对比维度**：在 0 到 1.0 的不同干扰率 $R$ 下，对比近距离空中碰撞（NMAC）次数和最小间隔距离。
*   **消融实验**：分别移除“不变性正则化”和“锚定正则化”，验证各组件对训练稳定性和最终性能的贡献。

#### 4. 资源与算力
*   **训练参数**：训练步数为 $2 \times 10^6$ 步，采用共享 Actor-Critic 架构，包含多头注意力机制（Multi-head Attention）处理变数量的邻近机群。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或具体的训练时长。但从算法复杂度来看，由于采用了闭式解而非迭代对抗训练，其算力需求应显著低于传统的鲁棒强化学习方法。

#### 5. 实验数量与充分性
*   **实验规模**：每组评估均基于 100 次随机模拟实验的平均值，并给出了标准差（阴影区域）。
*   **覆盖范围**：实验覆盖了从完全可靠（R=0）到极端不可靠（R=1.0）的全量程干扰水平。
*   **充分性评价**：实验设计较为充分。通过消融实验证明了单一正则化项的局限性（如仅用不变性正则化会导致训练崩溃），体现了实验的客观性。同时，在高密度流量（Poisson 到达过程）下的验证增加了结果的说服力。

#### 6. 主要结论与发现
*   **极强的鲁棒性**：在干扰率高达 35% 的情况下，鲁棒策略仍能保持近乎零的碰撞率（NMAC ≈ 0），而基准策略在干扰率超过 25% 后碰撞率急剧上升。
*   **优雅降级**：即使在极端干扰（R > 0.5）下，鲁棒策略的性能退化速度远慢于基准策略。
*   **无损性能**：在无干扰（R=0）的理想环境下，鲁棒策略的性能与标称策略相当，证明了鲁棒性提升并未以牺牲正常性能为代价。

#### 7. 优点
*   **计算高效**：推导出的闭式扰动解极大地简化了训练流程，使其能够扩展到大规模多智能体系统。
*   **理论严密**：不仅提出了算法，还通过数学证明给出了近似精度（二阶）和性能损失上界，具有较强的理论支撑。
*   **架构先进**：结合了注意力机制处理动态数量的邻近无人机，符合实际空中交通特征。

#### 8. 不足与局限
*   **假设限制**：方法依赖于价值函数的可微性假设，对于高度非连续或离散程度极高的状态空间可能适用性受限。
*   **攻击模型单一**：主要针对基于 $L_\infty$ 范数边界的随机/对抗扰动，未讨论非有界攻击或针对通信链路本身的协议攻击。
*   **实机验证缺失**：所有结论均基于 BlueSky 仿真环境，尚未在真实的物理无人机群或复杂的城市风场/遮挡环境中进行实测。
*   **超参数敏感性**：正则化系数（$\lambda_{inv}, \lambda_{anchor}$）的选取对训练稳定性至关重要，文中未详细讨论这些参数在不同场景下的泛化性。

（完）
