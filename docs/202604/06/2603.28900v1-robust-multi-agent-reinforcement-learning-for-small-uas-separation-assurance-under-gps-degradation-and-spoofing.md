---
title: Robust Multi-Agent Reinforcement Learning for Small UAS Separation Assurance under GPS Degradation and Spoofing
title_zh: 针对 GPS 性能下降与欺骗下小型无人机系统间隔保证的鲁棒多智能体强化学习
authors: "Alex Zongo, Filippos Fotiadis, Ufuk Topcu, Peng Wei"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28900v1"
tags: ["query:marl"]
score: 9.0
evidence: 多智能体强化学习中智能体与对手之间的零和博弈
tldr: 本研究针对小型无人机系统（sUAS）在GPS信号退化或欺骗环境下的间隔保证问题，提出了一种鲁棒多智能体强化学习（MARL）方法。通过将状态观测干扰建模为智能体与攻击者之间的零和博弈，推导出对抗扰动的闭式解，并将其集成到策略梯度算法中。该方法无需对抗训练即可实现线性时间评估，在保证安全性能的同时，显著提升了高密度无人机群在不可靠观测环境下的鲁棒性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.28900v1/fig-001.webp\", \"caption\": \"Fig. 4. Snapshot of high-density sUAS traffic under the robust policy at corruption rate R “ 0.35. Aircraft (triangles) traverse structured routes through waypoints, with separation buffers shown as circles. Yellow indicates aircraft approaching loss of separation; green indicates nominal separation status. Labels display flight ID, speed (knots), and altitude (ft). Despite adversarial GPS perturbations, all aircraft maintain safe separation.\", \"page\": 7, \"index\": 1, \"width\": 512, \"height\": 256}, {\"url\": \"assets/figures/arxiv/2603.28900v1/fig-002.webp\", \"caption\": \"Fig. 1. An en-route airspace setting for sUAS package delivery. WP = WayPoint. The UASs flying in this airspace need to maintain safe separation when they are impacted by degraded or spoofed GPS signal. The network includes two routes that first cross at WP 1 (in red), then merge later at WP 7 (light red). Each route spans about 10 km, illustrating a compact yet realistic urban scenario for sUAS operations.\", \"page\": 1, \"index\": 2, \"width\": 462, \"height\": 364}]"
motivation: 解决小型无人机在GPS受损或被欺骗时，因观测状态不可靠而导致的空中交通冲突和安全间隔失效问题。
method: 将状态干扰建模为零和博弈并推导出二阶精度的对抗扰动闭式解，随后将其集成到MARL策略梯度算法中进行鲁棒训练。
result: "在干扰水平高达35%的高密度无人机仿真中，该方法实现了近乎零的碰撞率，显著优于未经过对抗训练的基准策略。"
conclusion: 该研究通过数学推导简化了对抗训练过程，为受限观测环境下的多无人机协同安全控制提供了一种高效且具备理论保障的鲁棒方案。
---

## 摘要
本研究通过多智能体强化学习（MARL）解决了小型无人机系统（sUAS）在 GPS 性能下降和欺骗环境下的鲁棒间隔保证问题。在协同监视中，每架飞机（或智能体）广播其基于 GPS 获取的位置；当此类位置广播受到干扰时，整个观测到的空中交通状态将变得不可靠。我们将这种状态观测干扰建模为智能体与对手之间的零和博弈：对手以概率 R 扰动观测状态，以最大限度地降低每个智能体的安全性能。我们推导出了这种对抗性扰动的闭式表达式，从而完全绕过了对抗训练，并实现了状态维度上的线性时间评估。我们证明了该表达式以二阶精度逼近真实的最坏情况对抗扰动。此外，我们界定了干净观测与受损观测之间的安全性能差距，证明在 Kullback-Leibler 正则化下，该差距随干扰概率的增加至多呈线性退化。最后，我们将闭式对抗策略集成到 MARL 策略梯度算法中，以获得智能体的鲁棒应对策略。在高密度 sUAS 仿真中，我们在高达 35% 的干扰水平下观察到了接近零的碰撞率，其性能优于未经对抗扰动训练的基准策略。

## Abstract
We address robust separation assurance for small Unmanned Aircraft Systems (sUAS) under GPS degradation and spoofing via Multi-Agent Reinforcement Learning (MARL). In cooperative surveillance, each aircraft (or agent) broadcasts its GPS-derived position; when such position broadcasts are corrupted, the entire observed air traffic state becomes unreliable. We cast this state observation corruption as a zero-sum game between the agents and an adversary: with probability R, the adversary perturbs the observed state to maximally degrade each agent's safety performance. We derive a closed-form expression for this adversarial perturbation, bypassing adversarial training entirely and enabling linear-time evaluation in the state dimension. We show that this expression approximates the true worst-case adversarial perturbation with second-order accuracy. We further bound the safety performance gap between clean and corrupted observations, showing that it degrades at most linearly with the corruption probability under Kullback-Leibler regularization. Finally, we integrate the closed-form adversarial policy into a MARL policy gradient algorithm to obtain a robust counter-policy for the agents. In a high-density sUAS simulation, we observe near-zero collision rates under corruption levels up to 35%, outperforming a baseline policy trained without adversarial perturbations.

---

## 论文详细总结（自动生成）

### 论文总结：针对 GPS 性能下降与欺骗下小型无人机系统间隔保证的鲁棒多智能体强化学习

#### 1. 核心问题与整体含义（研究动机和背景）
随着小型无人机系统（sUAS）在城市物流和应急响应中的规模化应用，自主间隔保证（Separation Assurance）成为核心安全需求。然而，sUAS 高度依赖 GPS 进行定位和协同监视（如 Remote ID）。在城市环境中，GPS 信号易受多径效应干扰（误差可达 10-60m）或恶意欺骗攻击（成功率可达 5-40%）。
**核心问题**在于：当 GPS 受损时，无人机不仅自身定位错误，接收到的邻机广播位置也是错误的，导致整个观测状态矩阵发生关联性偏移。现有的多智能体强化学习（MARL）方法大多假设观测完美，在真实干扰下极易失效。本研究旨在开发一种在观测状态受损时仍能保持安全间隔的鲁棒策略。

#### 2. 论文提出的方法论
论文提出了一种结合博弈论与正则化技术的鲁棒 PPO 算法，核心思想是将观测干扰建模为智能体与对手之间的**零和博弈**。
*   **R-污染模型（R-contamination）**：假设观测值以 $1-R$ 的概率为真实状态，以 $R$ 的概率被替换为不确定集内的最坏情况对抗观测值。
*   **闭式对抗扰动（核心技术）**：研究者通过对价值函数 $V$ 进行一阶泰勒展开，推导出最坏情况扰动的闭式解：$\Xi^* = S - \kappa \cdot \text{sign}(\nabla V)$。该方法无需复杂的内层对抗训练，计算复杂度随状态维度线性增长，且证明具有二阶近似精度。
*   **扰动不变性正则化**：在 PPO 损失函数中引入两个 KL 散度项：
    1.  **不变性项（Invariance）**：强制策略在干净观测和对抗观测下输出相似的动作分布。
    2.  **锚定项（Anchoring）**：防止鲁棒训练使策略偏离预训练的标称（Nominal）教师策略，确保在无干扰时的性能。
*   **算法流程**：先在无干扰环境下预训练教师策略，随后在 $R$ 概率干扰环境下，利用闭式扰动生成对抗样本，结合 KL 正则化更新共享的 Actor-Critic 网络。

#### 3. 实验设计
*   **实验场景**：使用 BlueSky 空中交通模拟器构建了一个 10km 的城市包裹递送航线，包含交叉点和汇合点。
*   **无人机模型**：模拟 Amazon MK30 sUAS，具有非线性动力学约束。
*   **流量模式**：采用泊松过程生成高密度交通流。
*   **Benchmark（基准）**：对比了在完美观测下训练的**标称策略（Nominal Policy）**。
*   **对比维度**：在不同的干扰概率 $R$（0 到 1.0）下，对比近距离空中碰撞数（NMAC）和平均最小间隔距离。
*   **消融实验**：分别移除“不变性正则化”和“锚定正则化”，验证各组件对训练稳定性和鲁棒性的贡献。

#### 4. 资源与算力
*   **训练参数**：总训练步数为 $2 \times 10^6$ 步，学习率为 $2.5 \times 10^{-4}$，Batch Size 包含 8 个 Epoch。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或具体的训练耗时。但从算法描述看，由于采用了闭式解替代对抗训练，其计算开销应与标准 PPO 接近。

#### 5. 实验数量与充分性
*   **实验规模**：每组实验结果均基于 100 次随机模拟情节（Episodes）的平均值，并给出了标准差（阴影区域）。
*   **充分性**：实验覆盖了从 0% 到 100% 的全量程干扰水平，并进行了详细的消融实验。
*   **客观性**：通过设置不同的干扰阈值 $\kappa$（如位置 60m、速度 2m/s 等）来模拟真实的城市 GPS 误差，实验设计较为贴近实际物理环境，对比公平。

#### 6. 论文的主要结论与发现
*   **鲁棒性显著提升**：在干扰率 $R \le 0.35$ 的情况下，鲁棒策略几乎实现了零碰撞（NMAC $\approx 0$），而标称策略在 $R > 0.25$ 后碰撞率急剧上升。
*   **性能退化缓慢**：即使在极端干扰（$R=0.95$）下，鲁棒策略的碰撞数（约 5 次）远低于标称策略（约 18 次）。
*   **正则化的必要性**：消融实验显示，仅有不变性正则化会导致训练崩溃，仅有锚定项则无法应对高强度干扰，两者结合才能实现最佳平衡。
*   **理论验证**：数学证明显示，通过 KL 正则化约束决策偏差，可以使预期性能损失随干扰概率 $R$ 线性增长，而非指数级恶化。

#### 7. 优点
*   **计算高效**：推导出的闭式扰动解避免了昂贵的对抗样本搜索过程，适合实时性要求高的无人机系统。
*   **理论完备**：不仅给出了算法，还提供了关于近似精度（二阶）和性能损失上界的数学证明。
*   **去中心化执行**：虽然是集中式训练，但策略仅依赖局部观测，符合分布式无人机作业的实际需求。

#### 8. 不足与局限
*   **假设限制**：方法依赖于价值函数的可微性假设，对于高度非连续或离散的奖励函数可能表现不佳。
*   **扰动界限已知**：算法假设干扰的边界 $\kappa$ 是预先已知的，但在现实中，欺骗攻击的幅度可能是动态变化且不可预测的。
*   **场景单一**：实验主要集中在结构化航路（En-route），未测试在完全无结构的自由飞行（Free-flight）或极端天气干扰下的表现。
*   **算力细节缺失**：缺乏具体的硬件运行效率数据，难以评估其在嵌入式车载计算平台上的部署可行性。

（完）
