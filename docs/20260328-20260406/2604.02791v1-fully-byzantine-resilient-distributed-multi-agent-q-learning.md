---
title: Fully Byzantine-Resilient Distributed Multi-Agent Q-Learning
title_zh: 完全拜占庭容错的分布式多智能体 Q 学习
authors: "Haejoon Lee, Dimitra Panagou"
date: 2026-04-03
pdf: "https://arxiv.org/pdf/2604.02791v1"
tags: ["query:marl"]
score: 9.0
evidence: 拜占庭容错的分布式多智能体Q学习算法
tldr: 针对分布式多智能体强化学习在通信网络遭受拜占庭攻击时难以收敛至最优解的问题，本文提出一种新型分布式Q学习算法。该算法通过基于冗余的过滤机制，利用二跳邻居信息验证消息，确保所有智能体在拜占庭边缘攻击下仍能以概率1收敛至全局最优价值函数。研究还定义了新的网络拓扑条件并提供了多项式时间验证方法，克服了现有方法仅能达到次优解或依赖严苛假设的局限性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.02791v1/fig-001.webp\", \"caption\": \"Fig. 3. Performance comparison of our algorithm against the optimal Qvalues from the Oracle [6] (no attack) and the Baseline [22] under a 1-total Byzantine edge attack.\", \"page\": 7, \"index\": 1, \"width\": 512, \"height\": 447}]"
motivation: 现有的拜占庭容错多智能体强化学习算法通常只能收敛至次优解，无法保证在攻击下学习到全局最优策略。
method: 提出一种利用二跳邻居信息进行消息验证的冗余过滤机制，并引入了确保算法收敛的新型网络拓扑条件。
result: 理论证明了该算法在拜占庭边缘攻击下具有几乎处处收敛至最优解的特性，且其拓扑条件可在多项式时间内验证。
conclusion: 本研究实现了首个完全拜占庭容错的分布式Q学习框架，通过仿真验证了其在对抗环境下达成最优解的有效性。
---

## 摘要
我们研究了拜占庭容错的分布式多智能体强化学习（MARL），其中智能体必须在受损的通信网络上协作学习最优价值函数。现有的容错 MARL 方法通常只能保证以概率 1 收敛到次优价值函数，或者需要严苛的假设才能确保收敛到最优解。因此，在这些方法下，智能体可能无法学习到最优策略。为了解决这一问题，我们提出了一种新型分布式 Q 学习算法。在该算法下，尽管存在拜占庭边攻击，所有智能体的价值函数仍能以概率 1 收敛到最优价值函数。其核心思想是一种基于冗余的过滤机制，该机制利用二跳邻居信息来验证传入消息，同时保持双向信息流。随后，我们为该算法的收敛性引入了一种新的拓扑条件，提出了一种构建此类网络的系统方法，并证明了该条件可以在多项式时间内得到验证。我们通过仿真验证了研究结果，表明我们的方法能够收敛到最优解，而先前的方法在拜占庭边攻击下则会失效。

## Abstract
We study Byzantine-resilient distributed multi-agent reinforcement learning (MARL), where agents must collaboratively learn optimal value functions over a compromised communication network. Existing resilient MARL approaches typically guarantee almost sure convergence only to near-optimal value functions, or require restrictive assumptions to ensure convergence to optimal solution. As a result, agents may fail to learn the optimal policies under these methods. To address this, we propose a novel distributed Q-learning algorithm, under which all agents' value functions converge almost surely to the optimal value functions despite Byzantine edge attacks. The key idea is a redundancy-based filtering mechanism that leverages two-hop neighbor information to validate incoming messages, while preserving bidirectional information flow. We then introduce a new topological condition for the convergence of our algorithm, present a systematic method to construct such networks, and prove that this condition can be verified in polynomial time. We validate our results through simulations, showing that our method converges to the optimal solutions, whereas prior methods fail under Byzantine edge attacks.

---

## 论文详细总结（自动生成）

### 论文总结：完全拜占庭容错的分布式多智能体 Q 学习 (FRQD-Learning)

#### 1. 核心问题与整体含义（研究动机和背景）
在分布式多智能体强化学习（MARL）中，智能体通过通信网络共享局部信息以学习全局最优策略。然而，分布式系统极易受到**拜占庭攻击**（即攻击者可以任意篡改、伪造或丢弃通信信息）。
*   **研究动机**：现有的拜占庭容错 MARL 算法存在两个主要缺陷：一是大多只能保证收敛到“近优解”（near-optimal），导致智能体无法获得真正的最优策略；二是依赖于如“r-robustness”等极难验证的拓扑条件（验证此类条件属于 co-NP 完全问题），难以应用于大规模网络。
*   **核心目标**：设计一种算法，使所有智能体在存在拜占庭边缘攻击的情况下，仍能以概率 1（几乎处处）收敛到**精确的全局最优价值函数**。

#### 2. 方法论：核心思想与技术细节
论文提出了 **FRQD-learning**（Fully Resilient Distributed Q-learning）算法，其核心思想是利用**二跳（Two-hop）信息冗余**来验证消息的真实性。
*   **冗余过滤机制**：
    1.  **第一轮通信**：智能体向邻居广播自己的 Q 值。
    2.  **初步过滤**：智能体检查接收到的消息，剔除重复或异常的索引。
    3.  **第二轮通信（中继）**：智能体将从邻居处收到的 Q 值再次转发给自己的邻居。
    4.  **最终验证**：智能体收集来自不同路径的关于同一个目标智能体 $k$ 的 Q 值。如果某个特定数值出现的次数超过 $3F+1$（$F$ 为受攻击的边缘数量），则认为该值是可信的。
*   **关键技术条件**：引入了 **$(r, r')$-redundancy** 拓扑概念。该条件确保了网络中有足够的互联路径来提供冗余验证，同时保证了通信的对称性。
*   **收敛性证明**：通过将 FRQD 算法等效映射为在“r-2-hop 图”上的标准分布式 Q 学习，证明了其在拜占庭边缘攻击下的精确收敛性。

#### 3. 实验设计
*   **场景设定**：一个包含 10 个智能体（机器人）的异构协作任务分配场景。任务具有不同的操作成本和成功概率，状态空间包含任务进度，动作空间为机器人配对。
*   **Benchmark（基准）**：
    *   **Oracle**：无攻击环境下的原始分布式 Q 学习（代表理论上限）。
    *   **Baseline**：现有的拜占庭容错分布式 Q 学习方法（基于极端值过滤，如 [22, 24]）。
*   **攻击模型**：$F=1$ 的拜占庭边缘攻击，攻击者在两轮通信中分别注入极大的错误数值（如 10000）或伪造的消息集。

#### 4. 资源与算力
*   **算力说明**：论文中**未明确提及**具体的硬件资源（如 GPU 型号、数量）或具体的训练时长。
*   **算法复杂度**：论文从理论上指出，验证其提出的拓扑条件 $(r, r')$-redundancy 仅需 $O(n^3)$ 的计算复杂度，远优于现有方法的指数级复杂度。

#### 5. 实验数量与充分性
*   **实验规模**：主要进行了一组核心对比实验，展示了 Q 值随迭代次数的收敛曲线，并对比了各算法最终学习到的策略表。
*   **充分性评价**：实验设计较为**基础且具有针对性**。虽然没有进行大规模的消融实验或多场景测试，但其主要目的是为了验证理论证明的“精确收敛性”。实验结果清晰地展示了 Baseline 方法在攻击下偏离最优解，而 FRQD 完美重合了 Oracle 的曲线，有力支撑了核心论点。

#### 6. 主要结论与发现
*   **精确收敛**：FRQD-learning 是首个被证明在拜占庭边缘攻击下能以概率 1 收敛到精确最优价值函数的分布式算法。
*   **拓扑可验证性**：新提出的 $(r, r')$-redundancy 条件不仅能保证收敛，而且可以在多项式时间内完成验证，解决了大规模部署的障碍。
*   **策略有效性**：仿真表明，在 Baseline 方法因攻击导致策略错误（在 6 个状态中有 4 个状态选择了错误动作）时，FRQD 依然能识别出 100% 的最优策略。

#### 7. 优点
*   **理论突破**：打破了以往算法只能达到“近优”的局限，实现了“全容错”。
*   **计算友好**：拓扑验证算法的复杂度从 co-NP 完全降至多项式级（$O(n^3)$）。
*   **无需中心化**：完全去中心化，不需要可信的中央协调器。

#### 8. 不足与局限
*   **通信开销**：为了实现冗余验证，算法需要两轮通信，且第二轮的通信量达到 $O(|N_i|^2)$，在稠密网络中可能会产生较大的带宽压力。
*   **攻击模型限制**：目前主要针对**边缘攻击**（通信链路受损），虽然这涵盖了中间人攻击等常见场景，但对于**节点攻击**（智能体本身彻底叛变并协同作恶）的防御能力尚需进一步探讨。
*   **实验覆盖面**：实验仅限于离散动作空间的 Q 学习，尚未扩展到连续动作空间或深度强化学习（如 Actor-Critic 架构）的大规模复杂任务中。

（完）
