---
title: Internal State-Based Policy Gradient Methods for Partially Observable Markov Potential Games
title_zh: 基于内部状态的部分可观测马尔可夫势博弈策略梯度方法
authors: "Wonseok Yang, Thinh T. Doan"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.00433v1"
tags: ["query:marl"]
score: 9.0
evidence: 为马尔可夫势博弈实现自然策略梯度方法
tldr: 本文研究部分可观测马尔可夫势博弈（POMPGs）的多智能体强化学习问题。针对部分可观测性和信息分散带来的挑战，研究者利用公共信息框架并引入内部状态（有限状态控制器）来压缩历史信息，防止维度爆炸。通过提出基于内部状态的自然策略梯度方法，本文首次建立了非渐近收敛界，并将其分解为统计误差和近似误差。仿真结果表明，该方法在多种部分可观测环境下均优于仅使用当前观测的策略。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.00433v1/fig-001.webp\", \"caption\": \"\", \"page\": 4, \"index\": 1, \"width\": 3000, \"height\": 1879}, {\"url\": \"assets/figures/arxiv/2604.00433v1/fig-002.webp\", \"caption\": \"\", \"page\": 4, \"index\": 2, \"width\": 1256, \"height\": 480}]"
motivation: 针对部分可观测马尔可夫势博弈中信息分散和历史信息随时间无限增长导致的计算与收敛难题。
method: 结合公共信息框架与有限状态控制器，提出一种基于内部状态的自然策略梯度（NPG）算法。
result: 理论上建立了算法的非渐近收敛界，并将其分解为统计误差和有限状态控制器的近似误差。
conclusion: 实验证明，引入内部状态能有效提升智能体在部分可观测环境下的博弈性能，优于仅依赖当前观测的传统方法。
---

## 摘要
本文研究了部分可观测马尔可夫势博弈中的多智能体强化学习。由于部分可观测性、去中心化信息以及维数灾难，解决这一问题具有挑战性。首先，为了应对前两个挑战，我们利用了公共信息框架，该框架允许智能体基于共享信息和局部信息进行决策。其次，为了确保计算的可处理性，我们研究了一种能够压缩累积信息的内部状态，从而防止信息随时间无限增长。随后，我们实现了一种基于内部状态的自然策略梯度方法，用于寻找马尔可夫势博弈的纳什均衡。我们的主要贡献是为该方法建立了一个非渐近收敛界。该理论界限可分解为两个具有可解释性的部分：一个是在标准马尔可夫势博弈中也会出现的统计误差项，以及一个反映使用有限状态控制器所产生的近似误差项。最后，在多个部分可观测环境下的仿真实验表明，与仅使用当前观测值的设定相比，所提出的使用有限状态控制器的方法在性能上取得了持续的提升。

## Abstract
This letter studies multi-agent reinforcement learning in partially observable Markov potential games. Solving this problem is challenging due to partial observability, decentralized information, and the curse of dimensionality. First, to address the first two challenges, we leverage the common information framework, which allows agents to act based on both shared and local information. Second, to ensure tractability, we study an internal state that compresses accumulated information, preventing it from growing unboundedly over time. We then implement an internal state-based natural policy gradient method to find Nash equilibria of the Markov potential game. Our main contribution is to establish a non-asymptotic convergence bound for this method. Our theoretical bound decomposes into two interpretable components: a statistical error term that also arises in standard Markov potential games, and an approximation error capturing the use of finite-state controllers. Finally, simulations across multiple partially observable environments demonstrate that the proposed method using finite-state controllers achieves consistent improvements in performance compared to the setting where only the current observation is used.

---

## 论文详细总结（自动生成）

这篇论文由德克萨斯大学奥斯汀分校的研究团队完成，探讨了在部分可观测环境下多智能体强化学习（MARL）的收敛性与效率问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在**部分可观测马尔可夫势博弈（POMPGs）**中，克服信息去中心化、部分可观测性以及历史信息随时间无限增长（维度灾难）带来的挑战，找到纳什均衡（NE）。
*   **研究背景**：
    *   在完全可观测的势博弈中，梯度法已有成熟的收敛保证。
    *   但在部分可观测（Dec-POMDP）设定下，智能体需要依赖历史信息。虽然“公共信息框架”能将其转化为基于信念状态的 MDP，但信念空间是连续且不可数的，导致计算上不可行。
    *   **动机**：开发一种既能压缩历史信息（确保计算可处理性），又能保证收敛到纳什均衡的算法。

### 2. 方法论
*   **核心思想**：结合**公共信息框架**与**有限内部状态（Internal State）**表示，将无限增长的历史压缩为有限的内部状态，并在此基础上应用**自然策略梯度（NPG）**。
*   **关键技术细节**：
    *   **内部状态压缩**：引入共享内部状态 $w_k$（压缩公共历史）和局部内部状态 $l_k^i$（压缩局部记忆）。这些状态通过预定义的函数（如 RNN 或有限窗口）进行更新。
    *   **有限状态控制器（FSC）**：策略被限制在基于内部状态的类中，从而避免了对全量历史的依赖。
    *   **算法流程（Internal State NPG）**：
        1.  智能体根据当前内部状态和观测采取行动。
        2.  利用 Fisher 信息矩阵的逆来调整策略梯度，更新 Softmax 策略参数。
        3.  所有智能体同步更新，以最大化全局势函数。
*   **理论贡献**：建立了非渐近收敛界 $O(1/\sqrt{T}) + \epsilon_{FSC}$。其中 $\epsilon_{FSC}$ 是由于使用有限状态近似真实信念状态而产生的近似误差。

### 3. 实验设计
*   **实验场景**：使用了 **POSGGym** 基准测试库中的三个典型环境：
    1.  **Multi-Agent Tiger (MATiger)**：双人协作听声辨门，考验对噪声观测的处理。
    2.  **Multi-Access Broadcast Channel (MABC)**：多节点信道冲突避免，典型的协作通信场景。
    3.  **Level-Based Foraging (LBF)**：4x4 网格中的食物采集，需要智能体间的空间协调。
*   **对比基准（Benchmark）**：
    *   **有限存储策略（Finite-memory Policy）**：使用长度为 2 的滑动窗口作为内部状态。
    *   **反应式策略（Reactive Policy）**：仅依赖当前观测，不具备记忆能力。
*   **核心算法变体**：使用了带有参数共享的独立信任区域策略优化（ITRPO），这是 NPG 的一种实用变体。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的硬件配置（如 GPU 型号、数量）以及具体的训练时长。考虑到实验环境（POSGGym 的网格世界和简单博弈）规模较小，通常此类实验在单张消费级 GPU 或高性能 CPU 上即可完成。

### 5. 实验数量与充分性
*   **实验规模**：论文在三个不同的部分可观测环境中进行了测试，展示了学习曲线（Return vs Iterations）和行为可视化图。
*   **充分性评价**：
    *   **较充分**：涵盖了从简单博弈（Tiger）到空间协调（LBF）的不同维度，证明了内部状态在处理部分可观测性时的必要性。
    *   **局限性**：实验主要集中在协作型任务（共同奖励），虽然势博弈涵盖了这类任务，但对于更复杂的竞争性势博弈场景涉及较少。此外，内部状态的长度（$t_w=2$）较为固定，缺乏对不同记忆长度影响的消融实验。

### 6. 主要结论与发现
*   **内部状态的必要性**：在 MABC 等环境中，仅靠当前观测的反应式策略完全无法学习，而引入内部状态（记忆）后性能显著提升。
*   **收敛性验证**：实验结果与理论推导一致，学习曲线在经过一定迭代后趋于饱和，验证了 $O(1/\sqrt{T})$ 的收敛特性。
*   **协作涌现**：在 LBF 环境中，内部状态促使智能体产生了自然的协调行为（如对齐移动），证明了该方法能有效近似信念状态。

### 7. 优点
*   **理论与实践结合**：不仅提出了算法，还给出了严谨的非渐近收敛证明，这在部分可观测 MARL 领域较为难得。
*   **计算可处理性**：通过有限状态控制器解决了 Dec-POMDP 中状态空间爆炸的问题，为实际应用提供了可行路径。
*   **可解释性**：收敛界限被清晰地分解为统计误差和近似误差，便于理解算法性能的瓶颈。

### 8. 不足与局限
*   **结构依赖**：该方法严格依赖于环境具有“势博弈”结构，对于非势博弈的一般马尔可夫博弈，其收敛性无法保证。
*   **近似误差 $\epsilon_{FSC}$**：理论界限中存在一个无法消除的近似误差项，这意味着除非内部状态能够完美表征信念状态，否则无法达到绝对的最优纳什均衡。
*   **超参数敏感性**：内部状态压缩函数（如窗口长度或 RNN 结构）的选择对结果影响巨大，但论文未提供如何选择最优压缩结构的指导原则。

（完）
