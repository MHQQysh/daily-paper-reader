---
title: Learning Partial Action Replacement in Offline MARL
title_zh: 离线多智能体强化学习中的部分动作替换学习
authors: "Yue Jin, Giovanni Montana"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28573v1"
tags: ["query:marl"]
score: 9.0
evidence: 离线多智能体强化学习联合动作空间与PPO策略学习
tldr: 针对离线多智能体强化学习中联合动作空间爆炸和分布外动作导致的值估计偏差问题，本文提出PLCQL框架。该框架将部分动作替换（PAR）子集选择建模为上下文老虎机问题，利用PPO算法学习状态相关的自适应策略，动态决定替换智能体的数量。相比前作SPaCQL，PLCQL将计算开销从n次评估降至1次，并在多个基准测试中实现了更高的性能和效率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.28573v1/fig-001.webp\", \"caption\": \"Fig. 1. Illustration of the PLCQL algorithm.\", \"page\": 8, \"index\": 1, \"width\": 581, \"height\": 544}]"
motivation: 现有的部分动作替换方法计算成本高且无法随状态动态调整，难以应对离线MARL中指数级增长的动作空间和严重的分布外偏差。
method: 将PAR子集选择转化为上下文老虎机问题，并使用带有不确定性权重奖励的PPO算法来学习状态相关的自适应替换策略。
result: "在MPE、MaMuJoCo和SMAC等66%的任务中取得最高分，在84%的任务上超越了SPaCQL，同时显著降低了计算开销。"
conclusion: PLCQL通过自适应的动作替换策略，在保证保守值估计的同时提升了策略改进效率，是解决大规模离线MARL挑战的有效方案。
---

## 摘要
离线多智能体强化学习（MARL）面临着一个严峻挑战：联合动作空间随智能体数量呈指数级增长，导致数据集覆盖范围呈指数级稀疏，且不可避免地会出现分布外（OOD）联合动作。部分动作替换（PAR）通过将一部分智能体锚定在数据集动作上来缓解这一问题，但现有方法依赖于枚举多个子集配置，计算成本高昂，且无法适应不同的状态。我们提出了 PLCQL 框架，该框架将 PAR 子集选择建模为上下文老虎机（contextual bandit）问题，并使用带有不确定性权重奖励的近端策略优化（PPO）来学习状态相关的 PAR 策略。这种自适应策略在每个更新步骤中动态决定要替换多少个智能体，从而在策略改进与保守价值估计之间取得平衡。我们证明了一个价值误差界限，表明估计误差与偏离智能体的期望数量呈线性比例关系。与之前基于 PAR 的方法 SPaCQL 相比，PLCQL 将每次迭代的 Q 函数评估次数从 n 减少到 1，显著提高了计算效率。实验表明，PLCQL 在 MPE、MaMuJoCo 和 SMAC 基准测试的 66% 任务中获得了最高的归一化分数，在 84% 的任务中优于 SPaCQL，同时大幅降低了计算成本。

## Abstract
Offline multi-agent reinforcement learning (MARL) faces a critical challenge: the joint action space grows exponentially with the number of agents, making dataset coverage exponentially sparse and out-of-distribution (OOD) joint actions unavoidable. Partial Action Replacement (PAR) mitigates this by anchoring a subset of agents to dataset actions, but existing approach relies on enumerating multiple subset configurations at high computational cost and cannot adapt to varying states. We introduce PLCQL, a framework that formulates PAR subset selection as a contextual bandit problem and learns a state-dependent PAR policy using Proximal Policy Optimisation with an uncertainty-weighted reward. This adaptive policy dynamically determines how many agents to replace at each update step, balancing policy improvement against conservative value estimation. We prove a value-error bound showing that the estimation error scales linearly with the expected number of deviating agents. Compared with the previous PAR-based method SPaCQL, PLCQL reduces the number of per-iteration Q-function evaluations from n to 1, significantly improving computational efficiency. Empirically, PLCQL achieves the highest normalised scores on 66% of tasks across MPE, MaMuJoCo, and SMAC benchmarks, outperforming SPaCQL on 84% of tasks while substantially reducing computational cost.

---

## 论文详细总结（自动生成）

### 离线多智能体强化学习中的部分动作替换学习 (PLCQL) 论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
离线多智能体强化学习（Offline MARL）的核心挑战在于**联合动作空间的组合爆炸**。随着智能体数量增加，可能的动作组合呈指数级增长，导致有限的离线数据集只能覆盖极小比例的动作空间。这使得学习算法在评估分布外（OOD）联合动作时，容易产生严重的**值过估计**，导致策略失效。
虽然前人提出了“部分动作替换”（PAR）策略，通过将部分智能体的动作锚定在数据集内来缓解 OOD 问题，但现有方法（如 SPaCQL）存在两个缺陷：
*   **计算开销大**：需要枚举并评估所有可能的子集规模。
*   **缺乏灵活性**：无法根据当前环境状态动态调整替换策略。

#### 2. 方法论：核心思想与关键技术
本文提出了 **PLCQL (PAR Learning-based Conservative Q-Learning)** 框架，其核心思想是将 PAR 子集选择问题建模为一个**上下文老虎机（Contextual Bandit）**问题。

*   **核心思想**：学习一个状态相关的 PAR 策略 $\pi_{par}$，该策略根据当前状态 $s'$ 动态决定要替换的智能体数量 $k$。
*   **关键技术细节**：
    *   **上下文老虎机建模**：以观测到的状态为上下文，以替换数量 $k \in \{1, \dots, n\}$ 为动作。
    *   **不确定性权重奖励**：PAR 策略的奖励函数定义为 $r_{par} = \sigma(-u_Q \cdot T) \cdot Q(s', a'(k))$。其中 $u_Q$ 是 Q 函数集成（Ensemble）的标准差，用于衡量不确定性。这鼓励策略选择既能带来高价值、又具有低不确定性（即数据支持较好）的替换规模。
    *   **PPO 优化**：使用近端策略优化（PPO）算法来训练这个 PAR 策略，确保更新的稳定性。
    *   **PLCQL 目标函数**：结合了自适应 PAR 贝尔曼算子产生的 TD 误差和保守性惩罚项（借鉴自 CFCQL），在保证策略改进的同时抑制 OOD 动作的值。
*   **理论支撑**：论文证明了一个价值误差界限，显示估计误差与 PAR 策略诱导的期望偏离智能体数量呈线性相关。

#### 3. 实验设计
*   **数据集与场景**：
    *   **MPE (Multi-Agent Particle Environments)**：包括协作导航、捕食者-猎物等任务。
    *   **MaMuJoCo**：多智能体版本的 MuJoCo 连续控制任务（如 Half-Cheetah）。
    *   **SMAC (StarCraft Multi-Agent Challenge)**：星际争霸 II 微操任务，涵盖不同难度和智能体数量的地图（2s3z, 3s_vs_5z 等）。
*   **Benchmark 与对比方法**：
    *   对比了 9 种主流算法，包括策略约束类（OMAR, IQL, MA-TD3+BC, AWAC, MAICQ）、值约束类（MACQL, CFCQL, SPaCQL）以及基于扩散模型的方法（DoF）。

#### 4. 资源与算力
*   **硬件**：实验使用了 **NVIDIA Tesla V100 GPU**。
*   **软件实现**：基于 PyTorch。
*   **细节**：使用了 10 个 Q 网络组成的集成系统。
*   **说明**：文中明确提到 PLCQL 显著降低了计算成本，将每次迭代的 Q 函数评估次数从 $n$（智能体数量）降至 1，在 V100 上观察到了明显的墙钟时间（Wall-clock time）加速。

#### 5. 实验数量与充分性
*   **实验规模**：在 3 个基准测试的 32 个不同任务/数据集组合上进行了测试。
*   **重复性**：所有实验均使用 **5 个随机种子**运行，并报告了均值和标准差。
*   **消融实验**：针对“自适应学习”和“不确定性权重”两个核心组件进行了消融，验证了它们对性能的贡献。
*   **充分性评价**：实验覆盖了连续和离散动作空间、不同规模的智能体群体以及不同质量（Expert, Medium, Random）的数据集，对比基准全面，实验设计较为客观、公平。

#### 6. 主要结论与发现
*   **性能领先**：PLCQL 在 66% 的任务中取得了 SOTA（最高分）性能。
*   **优于前作**：在 84% 的任务中性能超过了之前的 PAR 标杆方法 SPaCQL。
*   **效率提升**：在提升性能的同时，大幅减少了计算开销，解决了 PAR 方法在大规模智能体系统中难以扩展的问题。
*   **自适应性**：实验证明，固定的替换数量 $k$ 在不同任务中表现不一，而 PLCQL 学习到的状态相关策略能自动找到最优平衡点。

#### 7. 优点
*   **高效性**：通过上下文老虎机简化了复杂的子集搜索问题，计算效率极高。
*   **自适应性强**：能够根据值函数的不确定性动态调整保守程度。
*   **理论与实践结合**：不仅有显著的实验提升，还提供了严谨的价值误差界限证明。

#### 8. 不足与局限
*   **子集采样简化**：虽然 $k$ 是学习得到的，但在确定 $k$ 后，具体的智能体子集仍是随机均匀采样的，未考虑智能体之间的特定依赖关系（如某些关键智能体可能更需要被锚定）。
*   **集成依赖**：算法依赖于 Q 函数集成来估计不确定性，当智能体数量极多时，维护大型集成网络仍可能面临内存和计算压力。
*   **应用限制**：主要针对离线设置，在在线或混合设置下的表现尚未探讨。

（完）
